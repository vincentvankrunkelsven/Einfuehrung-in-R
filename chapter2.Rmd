--- 
title_meta  : Kapitel 2
title       : Vektoren
description : "In diesem kostenlosen R-Kurs machen wir eine Reise nach Las Vegas. Hier lernen Sie, wie Sie Ihre Ergebnisse beim Glücksspiel unter Verwendung von Vektoren in R analysieren können! Am Ende dieses Kapitels werden Sie in der Lage sein, Vektoren in R zu erstellen, sie zu benennen, Elemente aus ihnen zu wählen und verschiedene Vektoren zu vergleichen."

--- type:NormalExercise xp:100 skills:1 key:d9b453dbdd
## Erstellen eines Vektors

Sind Sie ein Glückspilz? Dieses Kapitel nimmt Sie mit auf eine Reise in die Stadt der Sünde, auch bekannt als *Statisticians Paradise*!

Dank R und ihrer neuen datenanalytischen Fähigkeiten werden Sie lernen, wie Sie Ihre Leistung in den Spielcasinos und Ihre Karriere als Profi-Spieler verbessern. Dieses Kapitel zeigt Ihnen, wie Sie ganz leicht den Überblick über Ihren Spielerfolg erhalten und wie Sie einige einfache Analysen vorheriger Aktionen machen können. Nächster Halt: Vegas Baby... VEGAS!!

*** =instructions
- Erinnern Sie sich noch, was Sie im ersten Kapitel gelernt haben? Weisen Sie den Wert `"Go!"` der Variable `vegas` zu. Denken Sie in R daran: Klein- und Großschreibung!

*** =hint
Geben Sie die folgende Zeile im Editor ein:
```
vegas <- "Go!"
```

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Definieren Sie die Variable vegas
vegas <- 
```

*** =solution
```{r}
# Definieren Sie die Variable vegas
vegas <- "Go!"
```

*** =sct
```{r}
test_object("vegas", incorrect_msg = "Stellen Sie sicher, dass der richtige Wert für `vegas` gewählt wurde. Vergessen Sie nicht in R die Klein- und Großschreibung!")
success_msg("Sehr gut! Gehen Sie zur nächsten Übung.")
```


--- type:NormalExercise xp:100 skills:1 key:fd427db76f
## Erstellen eines Vektors (2)

Fokussieren wir uns zuerst!

Auf Ihrem Weg vom Tellerwäscher zum Millionär finden Sie eine umfangreiche Verwendung von Vektoren. Vektoren sind eindimensionale Arrays, die numerische Daten, Zeichendaten oder logische Daten sein können. Mit anderen Worten ist ein Vektor ein einfaches Werkzeug, um Daten zu speichern. Zum Beispiel können Sie Ihre täglichen Gewinne und Verluste in den Spielcasinos speichern.

In R erstellen Sie einen Vektor in mit der combine Funktion [`c()`](http://www.rdocumentation.org/packages/base/functions/c). Die Vektorelemente werden in der runden Klammer mit einem Komma getrennt. Zum Beispiel:

```
numeric_vector <- c(1, 2, 3)
character_vector <- c("a", "b", "c")
```

Sobald Sie diese Vektoren in R erstellt haben, können Sie diese nutzen, um Berechnungen durchzuführen.

*** =instructions 
Vervollständigen Sie den Code, so dass `boolean_vector` die drei Elemente: `TRUE`, `FALSE` and `TRUE` (in dieser Reihenfolge) enthält. 

*** =hint 
Weisen Sie `c(TRUE, FALSE, TRUE)` der Variable `boolean_vector` mit dem Operator `<-` operator zu.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Vervollständigen Sie den Code für boolean_vector
boolean_vector <-
```

*** =solution
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Vervollständigen Sie den Code für boolean_vector
boolean_vector <- c(TRUE, FALSE, TRUE)
```

*** =sct
```{r}
msg <- "Ändern Sie nicht den Code der `numeric_vector` und `character_vector` definiert!"
test_object("numeric_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("character_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("boolean_vector",
            incorrect_msg = "Vergewissern Sie sich, dass Sie `boolean_vector` die richtigen Werte zugewiesen haben. Verwenden Sie `c(TRUE, FALSE, TRUE)`. Machen Sie keine Anführungszeichen um `TRUE` und `FALSE`! und stellen Sie sicher, dass Sie dies selbe Reihenfolge, wie in der Beschreibung, verwendet haben.")
success_msg("Perfekt! Beachten Sie, dass ein Leerzeichen hinter dem Komma in der Funktion `c()` die Lesbarkeit Ihres Codes verbessert! Lassen Sie uns mehr Vektoren in der nächsten Übung erstellen.")
```


--- type:NormalExercise xp:100 skills:1 key:9f41229dbc
## Erstellen eines Vektors (3)

Eine Woche in Las Vegas und es befinden sich immer noch keine Ferraris in Ihrer Garage. Sie entschließen sich dazu, dass es endlich Zeit ist, Ihre datenanalytischen Supermächte zu nutzen.

Bevor Sie eine erste Analyse machen, sammeln Sie alle Gewinne und Verluste von der letzten Woche:

Für `poker_vector`: 

- Am Montag gewannen Sie $140
- Am Dienstag verloren Sie $50
- Am Mittwoch gewannen Sie $20 
- Am Donnerstag verloren Sie $120
- Am Freitag gewannen Sie $240

Für `roulette_vector`: 

- Am Montag verloren Sie $24
- Am Dienstag verloren Sie $50
- Am Mittwoch gewannen Sie $100
- Am Donnerstag verloren Sie $350
- Am Freitag gewannen Sie $10

Sie spielten nur Poker und Roulette, da nur diese Tische frei waren. Um diese Daten in R zu nutzen, entscheiden Sie sich die Variablen `poker_vector` und `roulette_vector` zu erstellen.

*** =instructions
Ordnen Sie die Gewinne/ Verluste für Roulette der Variable `roulette_vector` zu.

*** =hint
Um Ihnen bei diesem Schritt zu helfen, schauen Sie sich die Variable `poker_vector` (im Editor) an. Weisen Sie `roulette_vector`, basierend auf den Zahlen in der Beschreibung, die richtigen Werte zu. Vergessen Sie nicht, dass die Verluste negative Zahlen sind.

*** =sample_code
```{r}
# Poker Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette Gewinne von Montag bis Freitag
roulette_vector <-  
```

*** =solution
```{r}
# Poker Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette Gewinne von Montag bis Freitag
roulette_vector <- c(-24, -50, 100, -350, 10)
```

*** =sct
```{r}
test_object("poker_vector",
            incorrect_msg = "Stellen Sie sicher, dass Sie die richtigen Werte für `poker_vector` zugewiesen haben.")
test_object("roulette_vector",
            incorrect_msg = "Stellen Sie sicher, dass Sie die richtigen Werte für `roulette_vector` zugewiesen haben. Beachten Sie auch die richtige Reihenfolge!")
success_msg("Sehr gut! Um den Inhalt Ihrer Vektoren zu überprüfen, können Sie die Variable in der Konsole eingeben und überprüfen. Fahren Sie bitte mit der nächsten Übung fort!")
```


--- type:NormalExercise xp:100 skills:1 key:3b0b80b192
## Benennen eines Vektors

Als Datenanalyst ist es sehr wichtig eine klare Sicht auf die Daten zu haben, die Sie nutzen. Zu verstehen, worauf sich jedes Element bezieht, ist deshalb entscheidend.

In der vorherigen Übung haben wir Vektoren mit Ihren Gewinnen über die Woche erstellt. Jedes Vektorelement bezieht sich auf einen Tag in der Woche. Es wäre schön, wenn Sie das im Vektor selbst zeigen könnten.

Sie können den Elementen eines Vektors einen Namen geben mit der Funktion `names()`. Werfen Sie einen Blick auf dieses Beispiel:

```
some_vector <- c("John Doe", "poker player")
names(some_vector) <- c("Name", "Profession")
```

Zuerst wird ein Vektor `some_vector` erstellt, der zwei Elemente beinhaltet. Dem ersten Element wird der Name `Name` zugewiesen, während das zweite Element als `Profession` bezeichnet wird. In der Konsole wird folgende Ausgabe herausgegeben:

```
          Name     Profession 
    "John Doe" "poker player" 
```

*** =instructions
- Die Codeelemente auf der rechten Seite von `poker_vector` sind die Wochentage. Fügen Sie auch für `roulette_vector` die Codeelemente hinzu. 

*** =hint
Sie können `names(roulette_vector)` nutzen, um die Namen der Variable `roulette_vector` einzustellen. Stellen Sie sicher, dass Sie für die Vektoren die richtigen Wochentage zugeordnet haben. Achten Sie in R auf die Klein- und Großschreibung!

*** =sample_code
```{r}
# Poker Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette Gewinne von Montag bis Freitag
roulette_vector <- c(-24, -50, 100, -350, 10)

# Weisen Sie die Tage dem poker_vector zu
names(poker_vector) <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")

# Weisen Sie die Tage dem roulette_vectors zu
```

*** =solution
```{r}
# Poker Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette Gewinne von Montag bis Freitag
roulette_vector <- c(-24, -50, 100, -350, 10)

# Weisen Sie die Tage dem poker_vector zu
names(poker_vector) <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")

# Weisen Sie die Tage dem roulette_vectors zu
names(roulette_vector) <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
```

*** =sct
```{r}
test_object("poker_vector",
            incorrect_msg = "Ändern Sie nicht die Werte in `poker_vector`; sie wurden bereits codiert.")
test_object("roulette_vector",
            incorrect_msg = "Ändern Sie nicht die Werte in `roulette_vector`; sie wurden bereits codiert.")
test_object("poker_vector",
            eq_condition = "equal",
            incorrect_msg = "Ändern Sie nicht den Code, der die Elemente in `poker_vector` benennt; konzentrieren Sie sich auf `roulette_vector`!")
test_object("roulette_vector",
            eq_condition = "equal",
            incorrect_msg = "Stellen Sie sicher, dass Sie den richtigen Vektornamen für `roulette_vector` zugewiesen haben. Nutzen Sie exakt den gleichen Vektor, wie für `poker_vector`.")
success_msg("Gut gemacht! Gehen Sie zur nächsten Übung.")
```

--- type:NormalExercise xp:100 skills:1 key:6858c65a4a
## Benennen eines Vektors (2)

Wenn Sie ein guter Data Scientist sein wollen, müssen Sie faul werden. (Sind Sie bereits faul, dann haben Sie gute Chancen eines der außergewöhnlichen Naturtalente im Bereich Data Scientist zu sein.)
                                                                     
In der vorherigen Übung haben Sie vermutlich erfahren, wie frustrierend und langweilig es ist, die Informationen der Wochentage einzugeben. Allerdings, wenn man es aus einer höheren Perspektive betrachtet, ist es eine effizientere Art und Weise, die Vektoren der Wochentage einer **variable** zuzuweisen! 


*** =instructions
- Die Variable `days_vector` wurde bereits für Sie erstellt. Sie beinhaltet die Wochentage.
- Verwenden Sie `days_vector`, um die Namen für `poker_vector` und `roulette_vector` festzulegen.

*** =hint
Sie können `names(poker_vector) <- days_vector` benutzen, um den Namen des `poker_vector` einzustellen. Machen Sie das Gleiche für `roulette_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette Gewinne von Montag bis Freitag
roulette_vector <- c(-24, -50, 100, -350, 10)

# Die Variable days_vector
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
 
# Weisen Sie die Tage dem roulette_vectors und dem poker_vector zu
names(poker_vector) <-   
names(roulette_vector) <-
```

*** =solution
```{r}
# Poker Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette Gewinne von Montag bis Freitag
roulette_vector <- c(-24, -50, 100, -350, 10)

# Die Variable days_vector
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")

# Weisen Sie die Tage dem roulette_vectors und dem poker_vector zu
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector
```

*** =sct
```{r}
msg <- "Verändern Sie nicht die vordefinierten Variablen `poker_vector`, `roulette_vector` oder `days_vector`."
test_object("poker_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)

test_object("poker_vector",
            eq_condition = "equal",
            incorrect_msg = "Stellen Sie sicher, dass Sie `days_vector` als Namen von `poker_vector` zugewiesen haben.")
test_object("roulette_vector",
            eq_condition = "equal",
            incorrect_msg = "Stellen Sie sicher, dass Sie `days_vector` als Namen für `roulette_vector` zugewiesen haben.")
success_msg("Schön! Ein Rat: Versuchen Sie Verdopplung von Code für alle Zeit zu vermeiden. Machen Sie mit der nächsten Übung, 'wie man mit Vektoren rechnet', weiter!")
```


--- type:NormalExercise xp:100 skills:1 key:da995f099f
## Berechnung des Gesamtgewinns

Nun, da Sie die Poker- und Roulettegewinne als Vektoren haben, können Sie beginnen die Daten zu analysieren.

Sie wollen folgende Informationen herausfinden:

- Wie viel Gewinn oder Verlust haben Sie pro Tag in der Woche gemacht?
- Haben Sie in der Woche Geld verloren?
- Haben Sie in Poker oder in Roulette Geld gewonnen/verloren?

Um die Antwort zu bekommen, müssen Sie arithmetische Berechnungen auf Vektoren durchführen.

Es ist wichtig, wenn Sie zwei Vektoren in R zusammenfassen, dann elementeweise zu summieren. Zum Beispiel sind folgende drei Erklärungen völlig äquivalent:

```
c(1, 2, 3) + c(4, 5, 6)
c(1 + 4, 2 + 5, 3 + 6)
c(5, 7, 9)
```

Sie können auch die Berechnungen mit den Variablen, die die Vektoren darstellen, machen.

```
a <- c(1, 2, 3) 
b <- c(4, 5, 6)
c <- a + b
```

*** =instructions
- Nehmen Sie die Summe der Variablen `A_vector` und `B_vector` und weisen Sie dem `total_vector` zu.
- Überprüfen Sie das Ergebnis durch den Ausdruck von `total_vector`.

*** =hint
Benutzen Sie den Operator `+`, um `A_vector` und `B_vector` zu summieren. Verwenden Sie den Operator `<-` um das Ergebnis dem `total_vector` zuzuweisen.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Nehmen Sie die Summe von A_vector und B_vector
total_vector <- 
  
# Geben Sie total_vector aus

```

*** =solution
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Nehmen Sie die Summe von A_vector und B_vector
total_vector <- A_vector + B_vector

# Geben Sie total_vector aus
total_vector
```

*** =sct
```{r}
msg <- "Verändern Sie nicht den Inhalt von `A_vector` oder `B_vector`!"
test_object("A_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("B_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("total_vector", incorrect_msg = "Stellen Sie sicher, dass `total_vector` die Summe von `A_vector` und `B_vector` ergibt.")
test_output_contains("total_vector", incorrect_msg = "Vergessen Sie nicht, `total_vector` auszugeben! Schreiben Sie einfach `total_vector` in die neue Zeile.")
success_msg("Gute Arbeit! Weiter zur nächsten Übung.")
```


--- type:NormalExercise xp:100 skills:1 key:2969d8ed65
## Berechnung des Gesamtgewinns (2)

Jetzt verstehen Sie, wie R mit Vektoren rechnet. Es ist Zeit, dass die Ferraris in Ihre Garage kommen! Zuerst müssen Sie wissen, was der Gesamtgewinn oder -verlust pro Wochentag war. Der tägliche Gesamtgewinn ist die Summe von Gewinnen/Verlusten, die Sie pro Tag beim Pokern und beim Roulette realisiert haben. 

In R ist das nur die Summe von `roulette_vector` und `poker_vector`.

*** =instructions
Weisen Sie der Variable `total_daily`, wie viel Geld Sie insgesamt gewonnen oder verloren haben (Poker und Roulette kombiniert).

*** =hint
Ähnlich wie bei der vorherigen Übung, weisen Sie die Summe von zwei Vektoren einer neuen Variablen `total_daily` zu.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

#  Weisen Sie dem total_daily zu, wie viel Gewinn/ Verlust Sie an jedem Tag gemacht haben
total_daily <- 
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Weisen Sie dem total_daily zu, wie viel Gewinn/ Verlust Sie an jedem Tag gemacht haben
total_daily <- poker_vector + roulette_vector
```

*** =sct
```{r}
msg = "Verändern Sie nicht die Definition und den Namen von `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)

test_object("total_daily", incorrect_msg = "Stellen Sie sicher, dass Sie die Summe von `poker_vector` und `roulette_vector` dem `total_daily` zugewiesen haben.")

success_msg("Sehr gut! Fahren Sie mit der nächsten Übung fort.")
```


--- type:NormalExercise xp:100 skills:1 key:e66a56b9f0
## Berechnung des Gesamtgewinns (3)

Auf der Grundlage der vorherigen Analyse, sieht es aus, als hätten Sie eine Mischung von guten und schlechten Tagen. Dies ist nicht, was Ihr Ego erwartet hat und Sie fragen sich, ob Sie Geld über die Woche verloren haben?

Eine Funktion, die Ihnen diese Frage beantwortet ist [`sum()`](http://www.rdocumentation.org/packages/base/functions/sum). Sie berechnet die Summe aller Elemente eines Vektors. Zum Bespiel berechnen Sie die Geldsumme, die Sie beim Poker gewonnen/ verloren haben:

```
total_poker <- sum(poker_vector)
```

*** =instructions
- Berechnen Sie die Geldsumme, die Sie beim Roulette gewonnen/ verloren haben und weisen Sie sie der Variable `total_roulette` zu.
- Nun, da Sie die Summe für Roulette und Poker gebildet haben, können Sie ganz einfach `total_week` (Summe von allen Gewinnen und Verlusten der Woche) bestimmen. 
- Geben Sie `total_week` aus.

*** =hint
Benutzen Sie die Funktion [`sum()`](http://www.rdocumentation.org/packages/base/functions/sum), um die Summe von `roulette_vector` zu bestimmen. `total_week` ist die Summe von `roulette_vector` und `poker_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Gesamtgewinn mit Poker
total_poker <- sum(poker_vector)

# Gesamtgewinn mit Roulette
total_roulette <-  

# Gesamtgewinn
total_week <- 

# Geben Sie total_week aus
  
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Gesamtgewinn mit Poker
total_poker <- sum(poker_vector)

# Gesamtgewinn mit Roulette
total_roulette <-  sum(roulette_vector)

# Gesamtgewinn
total_week <- total_roulette + total_poker

# Geben Sie total_week aus
total_week
```

*** =sct
```{r}
msg = "Ändern Sie nicht die Definition und den Namen von `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("total_poker", 
            incorrect_msg = "Stellen Sie sicher, dass Sie `total_poker` die Summe von `poker_vector` zugewiesen haben.")
test_object("total_roulette",
            incorrect_msg = "Stellen Sie sicher, dass Sie `total_roulette` die Summe von `roulette_vector` zugewiesen haben.")
test_object("total_week",
            incorrect_msg = "Stellen Sie sicher, dass Sie `total_week`, die Summe von beiden Vektoren: `total_roulette` und `total_poker` gebildet haben.")

test_output_contains("total_week", incorrect_msg = "Vergessen Sie nicht `total_week` in die neue Zeile zu schreiben und auszugeben.")
success_msg("Sehr gut. Auch wenn das ziemlich schlechte Nachrichten sind...")
```


--- type:NormalExercise xp:100 skills:1 key:f532f5332d
## Vergleich des Gesamtgewinns

Es sieht also so aus, als würden Sie Geld verlieren. Es ist Zeit, um Ihre Strategie zu überdenken und anzupassen. Dies wird einige tiefere Analysen erfordern... 

Nach einem kurzen Brainstorming im Jacuzzi Ihres Hotels, realisieren Sie, dass eine mögliche Erklärung Die sein könnte, dass sich Ihre Fähigkeiten beim Poker, nicht aber beim Roulette, verbessert haben. Vielleicht sind deswegen Ihre Gesamtgewinne beim Poker höher (or `>` ) als beim Roulette.

*** =instructions
- Berechnen Sie `total_poker` und `total_roulette`, wie in der vorherigen Übung. Verwenden Sie die Funktion `sum()` zweimal.
- Vergleichen Sie, ob Sie die Gesamtgewinne im Poker höher als beim Roulette sind. Geben Sie einfach das Ergebnis dieses Vergleichs aus. Was schließen Sie daraus? Sollten Sie sich auf Roulette oder Poker fokussieren?

*** =hint
- Teilweise haben Sie die Antwort auf diese Frage in der vorherigen Übung bereits berechnet!
- Wenn 6 größer als 5 ist, geben Sie `6 > 5` ein. Die Ausgabe ergibt einen logischen Wert (`TRUE` oder `FALSE`).

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Berechnen Sie die Gesamtsumme für Poker und Roulette
total_poker <-
total_roulette <-

# Überprüfen Sie, ob Sie einen höheren Gewinn beim Poker als beim Roulette erzielt haben 

```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Berechnen Sie die Gesamtsumme für Poker und Roulette
total_poker <- sum(poker_vector)
total_roulette <- sum(roulette_vector)

# Überprüfen Sie, ob Sie einen höheren Gewinn beim Poker als beim Roulette erzielt haben 
total_poker > total_roulette
```

*** =sct
```{r}
msg <- "Ändern Sie nicht die Definition und den Namen von `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)

test_object("total_poker", 
            incorrect_msg = "Stellen Sie sicher, dass Sie für `total_poker` die Summe von `poker_vector` zugewiesen haben. Benutzen Sie `sum()`.")
test_object("total_roulette",
            incorrect_msg = "Stellen Sie sicher, dass Sie für `total_roulette` die Summe von `roulette_vector` zugewiesen haben. Benutzen Sie `sum()`.")
test_output_contains("total_poker > total_roulette",
                     incorrect_msg = "Haben Sie den Vergleich richtig ausgeführt? Überprüfen Sie, ob `total_poker` größer als `total_roulette` ist, dann verwenden Sie `total_poker > total_roulette`.")
success_msg("Gute Arbeit! Machen Sie jetzt bitte mit der nächsten Übung weiter.")
```


--- type:NormalExercise xp:100 skills:1 key:8d78be44e9
## Vektorauswahl: Die guten Zeiten

Ihr Gefühl schien richtig zu sein. Es scheint so, dass das Pokerspiel Ihnen mehr liegt als Roulette.

Ein anderer möglicher Ansatz für die Untersuchung ist es, Ihre Leistung am Anfang der Woche mit dem Ende der Woche zu vergleichen. 

Um diese Frage zu beantworten, konzentrieren Sie sich auf eine Auswahl des Vektors `total_vector`. Mit anderen Worten gesagt ist unser Ziel, bestimmte Elemente des Vektors zu wählen. Um die Elemente des Vektors (und später Matrizen, Datenframe) auszuwählen, können Sie die eckigen Klammern verwenden. Zwischen den eckigen Klammern geben Sie die Elemente an, die Sie auswählen. Zum Beispiel, um das erste Element des Vektors zu wählen, geben Sie `poker_vector[1]` ein. Um das zweite Element des Vektors zu wählen, geben Sie `poker_vector[2]` ein, etc. Beachten Sie, dass das erste Element in einem Vektor den Index 1 hat, nicht 0, wie in vielen anderen Programmiersprachen.

*** =instructions
Weisen Sie die Poker Ergebnisse von Mittwoch der Variable `poker_wednesday` zu.

*** =hint
Mittwoch ist das dritte Element von `poker_vector` und kann somit mit `poker_vector[3]` ausgewählt werden.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Definieren Sie eine neue Variable basierend auf einer Auswahl
poker_wednesday <- 
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Definieren Sie eine neue Variable basierend auf einer Auswahl
poker_wednesday <- poker_vector[3]
```

*** =sct
```{r}
msg = "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_wednesday", 
            undefined_msg = "Bitte stellen Sie sicher, dass Sie eine Variable `poker_wednesday` definieren.",
            incorrect_msg = "Es scheint so, dass `poker_wednesday` nicht den korrekten Wert des `poker_vector` enthält.")
success_msg("Sehr gut! R macht es möglich, mehrere Elemente eines Vektors auf einmal auszuwählen. Das erfahren Sie in der nächsten Übung!")
```


--- type:NormalExercise xp:100 skills:1 key:1351521670
## Vektorauswahl: Die guten Zeiten (2)

Wie wäre es, Ihre Ergebnisse der Wochenmitte zu analysieren?

Um mehrere Elemente eines Vektors auszuwählen, können Sie die eckigen Klammern am Ende hinzufügen. Sie können zwischen den Klammern zeigen, welche Elemente ausgewählt werden sollen. Zum Beispiel: Angenommen Sie wollen den ersten und den fünften Tag der Woche wählen: dann benutzen Sie den Vektor `c(1, 5)` zwischen den eckigen Klammern. Zum Beispiel wählt der Code das erste und fünfte Element von `poker_vector` aus:

```
poker_vector[c(1, 5)]
```

*** =instructions
Weisen Sie die Poker Ergebnisse vom Dienstag, Mittwoch und Donnerstag auf die Variable `poker_midweek` zu.

*** =hint
Verwenden Sie den Vektor `c(2, 3, 4)` zwischen den eckigen Klammern, um die korrekten Elemente von `poker_vector` auszuwählen.

*** =pre_exercise_code
```{r}
# no pec
``` 

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Definieren Sie eine neue Variable basierend auf einer Auswahl
poker_midweek <- 
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Definieren Sie eine neue Variable basierend auf einer Auswahl
poker_midweek <- poker_vector[c(2, 3, 4)]
```

*** =sct
```{r}
msg = "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_midweek", 
            incorrect_msg = "Es scheint so, dass `poker_midweek` nicht den korrekten Wert des `poker_vector` enthält. Sie können den Vektor `c(2, 3, 4)` innerhalb der eckigen Klammern verwenden.")
success_msg("Gut gemacht! Fahren Sie mit der nächsten Übung fort, um sich mehr in Vektorenauswahl zu spezialisieren!")
```


--- type:NormalExercise xp:100 skills:1 key:27976b79f4
## Vektorenauswahl: die guten Zeiten (3)

Das Auswählen mehrerer Elemente von `poker_vector` mit `c(2, 3, 4)` ist nicht sehr angenehm. Viele Datascientist sind faule Menschen, so dass Sie einen leichteren Weg aussuchen, um dies zu tun: `c(2, 3, 4)` kann mit `2:4` abgekürtzt werden, die einen Vektor mit allen natürlichen Nummern aus 2 bis zu 4 generieren.

Einen anderen Weg, um die Wochenmitte Ergebnisse zu finden, ist `poker_vector[2:4]`. Beachten Sie, wie der Vektor `2:4` zwischen den eckigen Klammern platziert wird, um die Elemente von 2 bis zu 4 zu wählen. 

*** =instructions
Weisen Sie dem `roulette_selection_vector` die Roulett Ergebnisse von Dienstag bis Freitag zu; nutzen Sie `:` damit es für Sie einfacher ist.

*** =hint
Weisen Sie eine Auswahl von `roulette_vector` dem `roulette_selection_vector` durch das Stellen von `2:5` zwischen den eckigen Klammern zu. 

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Definieren Sie eine neue Variable basierend auf einer Auswahl
roulette_selection_vector <- 
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Definieren Sie eine neue Variable basierend auf einer Auswahl
roulette_selection_vector <- roulette_vector[2:5]
```

*** =sct

```{r}
msg = "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_selection_vector", 
            undefined_msg = "Bitte stellen Sie sicher, dass Sie eine Variable `roulette_selection_vector` definieren.",
            incorrect_msg = Es scheint so, dass `roulette_selection_vector` nicht den korrekten Wert des `roulette_vector` enthält. Seien Sie sicher, dass Sie die richtigen Indexe verwendet haben.")
success_msg("Großartig! Awesome! Der Doppelpunkt Operator ist sehr nützlich und wird sehr oft in R Programmierung verwendet. Fahren Sie mit der nächsten Übung fort.")
```


--- type:NormalExercise xp:100 skills:1 key:e6c263ddee
## Vektorenauswahl: Die guten Zeiten (4)

Eine andere Möglichkeit, die vorherige Übung zu bewältigen ist das Verwenden von Namen der Vektorelemente (Montag, Dienstag, ...) statt ihrer numerischen Positionen. Zum Beispiel, 

```
poker_vector["Monday"]
```

wählt das erste Element von `poker_vector`, da `"Monday"` der Name des ersten Elements ist.

Genau wie Sie in der vorherigen Übung mit Numeric geübt haben, können Sie auch die Elementnamen verwenden, um mehrere Elemente auszuwählen, zum Beispiel: 

```
poker_vector[c("Monday","Tuesday")]
```

*** =instructions
- Wählen Sie die ersten drei Elemente in `poker_vector` durch ihre Namen: `"Montag"`, `"Dienstag"` und `"Mittwoch"`. Weisen Sie den Ergebnissen dem `poker_start` zu.
- Berechnen Sie den Durchschnitt der Werte in `poker_start` mit der Funktion [`mean()`](http://www.rdocumentation.org/packages/base/functions/mean). Geben Sie einfach das Ergebnis aus, so dass Sie es überprüfen können.

*** =hint
- Sie können `c("Montag", "Dienstag", "Mittwoch")` in eckigen Klammern verwenden, um einen Teil von `poker_vector` auszuwählen.
- Sie können `mean(poker_start)` verwenden, um den Mittelwert der Elemente in `poker_start` zu erhalten. Sie brauchen nicht den Mittelwert aller Poker Elemente, sondern nur von den ersten drei Tagen.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Wählen Sie die Ergebnisse für Montag, Dienstag und Mittwoch
poker_start <- 
  
# Berechnen Sie den Mittelwert der Elemente in poker_start

```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Wählen Sie die Ergebnisse für Montag, Dienstag und Mittwoch
poker_start <- poker_vector[c("Monday", "Tuesday", "Wednesday")]
  
# Berechnen Sie den Mittelwert der Elemente in poker_start
mean(poker_start)
```

*** =sct
```{r}
msg = "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_start", 
            incorrect_msg = "Es scheint so, dass `poker_start` nicht die ersten drei Werte von `poker_vector` enthält. Sie können `c(\"Monday\", \"Tuesday\", \"Wednesday\")` in eckigen Klammern verwenden.")
test_output_contains("mean(poker_start)", incorrect_msg = "Haben Sie den richtigen Durchschnittswert von `poker_start` berechnet und ausgegeben? Verwenden Sie `mean(poker_start)`.")
success_msg("Gute Arbeit! Vektoren können durch Index oder durch Namen ausgewählt werden, aber Sie können auch Vektoren gegenübergestellt werden. Die nächste Übung wird Ihnen zeigen wie!");
```


--- type:NormalExercise xp:100 skills:1 key:f0f619c901
## Auswahl durch einen Vergleich - Schritt 1

Durch die Verwendung von Vergleichsoperatoren können wir uns an die vorherige Frage annähern.

Logische Vergleichsoperatoren sind:

- `<` für weniger als
- `>` für mehr als
- `<=` für kleiner oder gleich
- `>=` für größer oder gleich
- `==` für gleich
- `!=` für ungleich

Wie im vorherigen Kapitel gesehen, gibt `6 > 5` `TRUE` zurück. Das Gute in R ist, dass Sie diese Vergleichsoperatoren auch auf Vektoren anwenden können. Zum Beispiel:

```
> c(4, 5, 6) > 5
[1] FALSE FALSE TRUE
```

Dieser Befehl testet für jedes Element des Vektors, ob die Bedingung, gegeben durch die Vergleichsoperatoren, `TRUE` oder `FALSE` ist.

*** =instructions
- Prüfen Sie, welche Elemente in `poker_vector` positiv sind (i.e. > 0) und ordnen Sie diese `selection_vector` zu. 
- Geben Sie `selection_vector` aus, damit Sie es überprüfen können. Die Ausgabe sagt Ihnen, ob Sie Geld (`TRUE`) gewonnen oder (`FALSE`) verloren haben.

*** =hint
Um die Tage zu finden, an denen ein positiver Gewinn in Poker gemacht wurde, wird mit R jedes Element in `poker_vector`(größer als 0) untersucht. `some_vector > 0` ist die Art und Weise, wie Sie in R die Antwort bekommen. 

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# An welchen Tagen machten Sie Geld in Poker?
selection_vector <- 
  
# Geben Sie selection_vector aus

```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# An welchen Tagen machten Sie Geld in Poker?
selection_vector <- poker_vector > 0
  
# Geben Sie selection_vector aus
selection_vector
```

*** =sct
```{r}
msg <- "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)

test_object("selection_vector", incorrect_msg = "Es scheint so, dass `selection_vector` nicht die korrekten Ergebnisse enthält. Denken Sie daran, dass R elementweise Abläufe für Vektoren verwendent.")
test_output_contains("selection_vector", incorrect_msg = "Vergessen Sie die Ausgabe von `selection_vector`, durch das Schreiben der Variablenname in eine neue Zeile, nicht.")
success_msg("Sehr gut!")
```


--- type:NormalExercise xp:100 skills:1 key:2754fc5cd4
## Auswahl durch einen Vergleich - Schritt 2

Das Arbeiten mit Vergleichen macht Ihr datenanalytisches Leben leichter. Statt eine Teilmenge von Tagen zu wählen (wie zuvor), können Sie ganz einfach R fragen, an welchen Tagen eine positive Rendite realisiert wurde.

In den vorherigen Übungen verwendeten Sie `selection_vector <- poker_vector > 0`, um die Tage zu finden, an denen Sie positive Einnahmen beim Pokern gemacht haben. 

Sie können die gewünschten Elemente durch das Platzieren von `selection_vector` zwischen den eckigen Klammern wählen:

```
poker_vector[selection_vector]
```

R weiß, was zu tun ist, wenn Sie einen logischen Vektor in eckigen Klammern setzen: R wird nur die Elemente auswählen, die `TRUE` in `selection_vector` entsprechen.

*** =instructions
Verwenden Sie `selection_vector` in eckigen Klammern, um der Variable `poker_winning_days` die Beträge zuzuordnen, die Sie an den profitablen Tagen gewannen. 

*** =hint
Verwenden Sie `poker_vector[selection_vector]`, um die gewünschten Elemente aus `poker_vector` auszuwählen und ordnen Sie die Ergebnisse `poker_winning_days` zu.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker and Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# An welchen Tagen machten Sie Geld in Poker?
selection_vector <- poker_vector > 0

# Wählen Sie diese Tage aus poker_vector
poker_winning_days <- 
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# An welchen Tagen machten Sie Geld beim Pokern?
selection_vector <- poker_vector > 0

# Wählen Sie diese Tage aus poker_vector
poker_winning_days <- poker_vector[selection_vector]
```

*** =sct
```{r}
msg = "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("selection_vector", incorrect_msg = "Ändern Sie nicht die Art und Weise, wie `selection_vector` berechnet ist.")
test_object("poker_winning_days",
            incorrect_msg =  "Es scheint so, dass `poker_winning_days` nicht die korrekten Ergebnisse enthält. Verwenden Sie `poker_vector[selection_vector]`.")
success_msg("Gute Arbeit! Machen Sie mit der nächsten Übung weiter.")
```


--- type:NormalExercise xp:100 skills:1 key:59e8dcbbd5
## Erweiterte Auswahl

Nun wollen Sie auch die Tage wissen, an denen Sie einen positiven Ertrag beim Roulette realisiert haben.

*** =instructions
- Erstellen Sie die Variable `selection_vector`, um dieses Mal zu sehen, ob Sie Gewinne beim Roulette an verschiedenen Tagen gemacht haben.
- Weisen Sie die Beträge, die Sie an den Tagen positiv beendeten, der Variable `roulette_winning_days` zu. Dieser Vektor enthält den positiven Gewinn von `roulette_vector`.

*** =hint
Sobald Sie `selection_vector` richtig berechnet haben, können Sie wieder `roulette_vector[selection_vector]` verwenden, um die positiven Ergebnisse von `roulette_vector` zu wählen..

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# An welchen Tagen machten Sie Geld beim Roulette?
selection_vector <-

# Wählen Sie diese Tage aus roulette_vector
roulette_winning_days <- 
```

*** =solution
```{r}
# Poker und Roulette Gewinne von Montag bis Freitag:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Montag", "Dienstag", "Mittwoch", "Donnerstag", "Freitag")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# An welchen Tagen machten Sie Geld in Roulette?
selection_vector <- roulette_vector > 0

# Wählen Sie diese Tage aus roulette_vector
roulette_winning_days <- roulette_vector[selection_vector]
```

*** =sct
```{r}
msg = "Ändern Sie nicht die Definition und den Namen des `poker_vector` und `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("selection_vector", 
            incorrect_msg = "Es scheint so, dass `selection_vector` nicht die korrekten Ergebnisse enthält. Verwenden Sie `roulette_vector > 0`.")
test_object("roulette_winning_days",
            incorrect_msg = " Es scheint so, dass `roulette_winning_days` nicht die korrekten Ergebnisse enthält. Verwenden Sie `roulette_vector[selection_vector]`.")

success_msg("Sehr gut! Diese Übung beschließt das Kapitel über Vektoren. Das nächste Kapitel wird Ihnen die zweidimensionale Version von Vektoren vorstellen: Matrizen.")
```





